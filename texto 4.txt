<!DOCTYPE html>
<html>
<head>
  <title>Painel de Sinais</title>
  <script src="https://cdn.socket.io/4.0.1/socket.io.min.js"></script>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="p-3 bg-light">
  <h2 class="mb-3">📊 Sinais em Tempo Real</h2>
  <ul id="sinais" class="list-group"></ul>

  <script>
    var socket = io();
    socket.on("novo_sinal", function(data) {
        let li = document.createElement("li");
        li.className = "list-group-item " + (data.direcao=="CALL" ? "list-group-item-success" : "list-group-item-danger");
        li.innerHTML = "<b>" + data.ativo + "</b> → " + data.direcao + " às " + data.horario;
        document.getElementById("sinais").prepend(li);
    });
  </script>
</body>
</html>